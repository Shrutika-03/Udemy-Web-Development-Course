// && => AND
// || => OR
// ! => reverse the result

let isLoggedIn = true;

let isPaid = false;

// need both condition will be true/fulfill the demand
// console.log(isLoggedIn && isPaid);

let isEmailUser = true;
let isGoogleUser = false;

// need one condition has to be true
console.log(isEmailUser || isGoogleUser);
