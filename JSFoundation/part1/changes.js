var petName = "Cat";

// petName = "Dog"; --- it's applicable with VAR keyword to re-assign the value

// var petName = "Tiger "; ---- it's also applicable as to re-create the variable with same name using VAR keyword

let gameName = "spiderman";

gameName = "batman"; //---- it's applicable with let keyword

// let gameName = "pull"; ----- not applicable to create a variable with same name as the variable name already exist
console.log(gameName);

const username = "Shrutika";
// username = "diva"; it's not applicable in CONST keyword
// const username="Jiva" it's also not applicable to create same name variable with CONST keyword as already exist
console.log(username);
